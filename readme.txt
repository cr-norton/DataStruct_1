run at command with ./prog < input
file should have commands separated by newline otherwise it has errors with input. Can't all be in one line.

Should all work. Test file is perfect but we'll see with the rest.

One file with all the code. Graph class includes an AVLTree


Cheers
